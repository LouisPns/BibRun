 <!-- current_user.inspect  -->

 <div class="profile-header-wrapper" style="background: linear-gradient(rgba(0,0,0,0.4), rgba(0,0,0,3)), url('urban_trail.jpg');">
  <div class="container profile-header-container text-center">
    <div class="profile-header-infos">
      <%= image_tag current_user.picture, class: "avatar dropdown-toggle", id: "navbar-wagon-menu", "data-toggle" => "dropdown" %>
      <h1><%= current_user.first_name%> <%= current_user.last_name%></h1>
      <span class="label label-info">New bib</span>
      <h2><%= current_user.level%></h2>
    </div>

          <div class="tabs">
           <a class="tab active" href="#">
            <h3>My bibs & sales</h3>
            <p><%=Bib.count%> bib </p>
          </a>
          <a class="tab" href="#">
            <h3>My Purchases</h3>
            <p><%=Order.count%> bib</p>
          </a>
        </div>
      </div>
      </div>
      <% @bibs.each do |bib| %>
      <div class="container">
        <div class="product">
          <img src="cross.jpg" alt="race" class="product-image">
          <div class='product-body'>
            <h3><%=bib.race.name%></h3>
            <p><%=bib.race.date%>| <%=bib.race.race_type%> | <%=bib.race.distance%> km | <%=bib.race.label%></p>
            <h2><span class="label label-info"><%= bib.price %>€</span></h2>
            <span class="label label-default">Sold to Louis (10/04/2016)</span>
          </div>
        </div>
    <% end %>



